Fix Quill multi-block selection formatting and font dropdown

- Register custom fonts (serif, monospace, sans-serif) so font dropdown works
- Add custom toolbar handlers using formatText() to apply font, size, bold,
  italic, underline, strike, color, and background across multi-block selections
- Add !important to size CSS classes so they override header font-size
- Add font-family override CSS so font dropdown works inside headers
- Keep selection after applying format so toolbar stays in sync

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
